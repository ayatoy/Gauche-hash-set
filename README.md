# Simple hash-set for Gauche

Gauche 0.9.2

## Install

    $ git clone git://github.com/ayatoy/Gauche-hash-set.git
    $ cd Gauche-hash-set
    $ ./DIST gen
    $ ./configure
    $ make check
    $ [sudo] make install
