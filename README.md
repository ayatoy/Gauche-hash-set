# Simple hash-set for Gauche

Gauche 0.9.2

## Install

    $ git clone git://github.com/ayatoy/Gauche-extra-set.git
    $ cd Gauche-extra-set
    $ ./DIST gen
    $ ./configure
    $ make check
    $ [sudo] make install
